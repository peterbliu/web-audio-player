{
  "name": "vario-speed-audio-tools",
  "version": "0.30.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 4111 --bind 127.0.0.1",
    "start": "npm run dev",
    "test": "node --test tests/*.test.mjs"
  }
}
